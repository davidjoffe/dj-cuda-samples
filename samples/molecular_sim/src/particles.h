// dj-cuda-samples — https://github.com/davidjoffe/dj-cuda-samples
// (c) David Joffe / DJ Software - Business Source License (BSL 1.1). See LICENSE
#pragma once

/*
//as-yet-unused but should wrap particle data in a struct or class
struct Particles
{
    float4* pos;   // (x, y, z, q)
    float4* vel;   // (vx, vy, vz, _)
    float4* force; // (fx, fy, fz, _)
};
*/